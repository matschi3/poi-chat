import {
  StyledFooter,
  StyledPlaceholder,
  StyledNavProfile,
} from "./Footer.styled";

export default function Footer() {
  return (
    <StyledFooter>
      <StyledPlaceholder />
      <StyledNavProfile />
    </StyledFooter>
  );
}
