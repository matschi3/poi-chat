import {
  StyledFooter,
  StyledPlaceholder,
  StyledNavProfile,
} from "./Footer.styled";

export default function Footer() {
  return (
    <StyledFooter>
      <StyledPlaceholder />
      <StyledPlaceholder />
      <StyledPlaceholder />
      <StyledPlaceholder />
      <StyledNavProfile />
    </StyledFooter>
  );
}
